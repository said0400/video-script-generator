# ══════════════════════════════════════════════════════════════════════════════
# requirements.txt — Video Script Generator
# ══════════════════════════════════════════════════════════════════════════════
#
# ⚠️  ملاحظات مهمة:
#
#   1. torch + torchaudio يُثبَّتان في GitHub Actions بشكل منفصل:
#      pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
#   2. whisperx يُثبَّت من GitHub في GitHub Actions:
#      pip install git+https://github.com/m-bain/whisperX.git
#
#   3. ffmpeg يُثبَّت كـ system package في GitHub Actions:
#      sudo apt-get install -y ffmpeg
#
#   4. لا تضع ffmpeg-python هنا — الكود يستخدم subprocess مباشرة
#
# ══════════════════════════════════════════════════════════════════════════════


# ── Google Gemini (TTS + AI) ──────────────────────────────────────────────────
google-genai>=0.8.0


# ── Groq (AI Enrichment) ──────────────────────────────────────────────────────
groq>=0.11.0


# ── Excel Reader ──────────────────────────────────────────────────────────────
openpyxl>=3.1.0


# ── HTTP Requests ─────────────────────────────────────────────────────────────
requests>=2.31.0


# ── Playwright (Thumbnail + Render) ───────────────────────────────────────────
# يُثبَّت هنا ثم يُشغَّل: python -m playwright install chromium --with-deps
playwright>=1.44.0


# ── WhisperX Core Dependencies ────────────────────────────────────────────────
# whisperx نفسه يُثبَّت في GitHub Actions من GitHub مباشرة
# لكن هذه المكتبات يجب أن تكون هنا لضمان التوافق:

faster-whisper>=1.0.0
ctranslate2>=4.4.0
transformers>=4.36.0
tokenizers>=0.15.0
huggingface_hub>=0.20.0


# ── PyAnnote (Word-level alignment) ───────────────────────────────────────────
# ⚠️  يحتاج HF_TOKEN في البيئة لتحميل النماذج من HuggingFace
# أضف HF_TOKEN في GitHub Secrets

pyannote.audio>=3.1.0
pyannote.core>=5.0.0
pyannote.pipeline>=3.0.1


# ── Audio Processing ──────────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.0
audioread>=3.0.0
scipy>=1.11.0


# ── Numpy (مقيّد لتوافق WhisperX) ────────────────────────────────────────────
# WhisperX لا يدعم numpy 2.x حتى الآن
numpy>=1.24.0,<2.0.0


# ── NLP ───────────────────────────────────────────────────────────────────────
nltk>=3.8.1


# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
more-itertools>=9.1.0
