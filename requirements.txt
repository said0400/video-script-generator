# ── Core Dependencies ────────────────────────────────────────────
google-genai
groq
openpyxl
requests
playwright

# ── ✨ WhisperX dependencies (compatible versions) ──────────────
faster-whisper==1.0.3
ctranslate2==4.4.0
transformers==4.39.3
pyannote.audio==3.1.1
numpy<2.0
ffmpeg-python

# ⚠️ ملاحظة: whisperx يُثبّت يدوياً في run.yml (بدون deps)
# لتجنب تحديث PyTorch
